import SetPasswordPage from "@/components/auth/SetPasswordPage";

export const metadata = { title: "Set your password — Pocket Jane" };

export default function SetPassword() {
  return <SetPasswordPage />;
}
